defaults:
  - _self_ # suppresses warning, recommended for default.yaml

initialized: false

### HYDRA_LOGGING_SETTINGS - toggle to suppress output
hydra_logging:
  loggers:
    logging_example:
      level: INFO
hydra:
  run:
    dir: ''
  output_subdir: null
  # job_logging:
  #   formatters:
  #     simple:
  #       format: '[%(levelname)ssss] - %(message)s'
  #   root:
  #     handlers: [console]

# zendron - default
zotero_comment_title: zendron comment
pod_path: zendron_pod
dendron_limb: zendron.import
item_types: [journalArticle, book, preprint, conferencePaper, report]
